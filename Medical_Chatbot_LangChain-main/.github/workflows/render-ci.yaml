# name: Lint and Validate

# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main]

# jobs:
#   validate:
#     runs-on: ubuntu-latest
    
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.10'

#       - name: Validate requirements.txt
#         run: |
#           python -m pip install --upgrade pip
#           # Just check if requirements are valid, don't install heavy packages
#           pip install --dry-run -r requirements.txt || echo "Requirements check completed"

#       - name: Check Python syntax
#         run: |
#           pip install flake8
#           # Check for syntax errors only
#           flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=venv,env,__pycache__,.git
#         continue-on-error: true

#       - name: Validate Dockerfile exists
#         run: |
#           if [ -f "Dockerfile" ]; then
#             echo "✅ Dockerfile found"
#           else
#             echo "❌ Dockerfile missing"
#             exit 1
#           fi

#       - name: Deployment note
#         run: |
#           echo "✅ Basic validation passed!"
#           echo "🚀 Render will build and deploy automatically"
